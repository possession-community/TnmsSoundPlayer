name: CI/CD

on:
  push:
    branches: [main]
    tags: ['v*']

jobs:
  cicd:
    uses: possession-community/modsharp-publish-action/.github/workflows/deploy.yml@v2
    # The NuGet push authenticates through OIDC rather than a stored key, and a reusable workflow
    # cannot grant itself the token — it only ever gets what this job hands down.
    permissions:
      contents: read
      id-token: write
    with:
      # TnmsSoundPlayerTest is a manual harness, so it is deliberately not shipped.
      projects: TnmsSoundPlayer
      shared-projects-phase1: TnmsSoundPlayer.Shared

      platforms: linux-x64 win-x64

      main-artifact-name: TnmsSoundPlayer
      main-artifact-include: modules shared

      nuget-project-dirs: TnmsSoundPlayer.Shared
      nuget-config-props: config.props
    secrets:
      NUGET_USER: ${{ secrets.NUGET_USER }}

  release:
    needs: cicd
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: release-artifacts-*
          merge-multiple: true
          path: artifacts/
      - env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --repo "${GITHUB_REPOSITORY}" \
            --title "Release ${GITHUB_REF_NAME}" \
            --generate-notes \
            artifacts/*.zip
